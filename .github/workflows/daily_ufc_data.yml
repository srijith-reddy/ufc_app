name: Daily UFC Cards & Odds Scrape

on:
  schedule:
    - cron: "0 6 * * *"   # Daily at 06:00 UTC
  workflow_dispatch:
    inputs:
      event_number:
        description: "Optional UFC event number (e.g. 324)"
        required: false

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      # -----------------------------
      # CHECKOUT
      # -----------------------------
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------
      # PYTHON
      # -----------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # -----------------------------
      # DEPENDENCIES
      # -----------------------------
      - name: Install dependencies
        run: |
          pip install -r requirements-scrape.txt
          playwright install chromium

      # -----------------------------
      # SCRAPE UFC CARD
      # (manual only — cards don't auto-detect safely)
      # -----------------------------
      - name: Scrape UFC fight card
        run: |
          if [ -z "${{ github.event.inputs.event_number }}" ]; then
            echo "No event number provided — skipping card scrape"
          else
            python scrape_cards.py ${{ github.event.inputs.event_number }}
          fi

      # -----------------------------
      # SCRAPE ODDS
      # (manual OR scheduled)
      # -----------------------------
      - name: Scrape UFC odds
        run: |
          if [ -z "${{ github.event.inputs.event_number }}" ]; then
            python scrape_odds.py
          else
            python scrape_odds.py ${{ github.event.inputs.event_number }}
          fi

      # -----------------------------
      # COMMIT & PUSH (SAFE)
      # -----------------------------
      - name: Commit and push data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add cards/ odds/
          git commit -m "Update UFC cards & odds" || echo "No changes to commit"

          git pull --rebase origin main
          git push origin main
