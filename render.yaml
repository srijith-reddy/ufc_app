services:
  - type: web
    name: octagon-intel-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements-backend.txt
    startCommand: uvicorn index:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
